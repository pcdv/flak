# default backend for running tests (other possible value: netty)
backend=jdk

# skips the configuration phase entirely when nothing that feeds it changed.
# It will be the default in Gradle 10
org.gradle.configuration-cache=true
