description = "Flak backend based on netty"

dependencies {
  implementation project(":flak-api")
  implementation project(":flak-spi")
  // exposed by NettyWebServer.getHttpHandler(), which an application plugs
  // into the pipeline of its own server
  api "io.netty:netty-codec-http:4.2.18.Final"
}