# flak-jackson

A Flak plugin converting handler arguments and return values from and to
JSON with Jackson: annotate a handler with `@JSON` and it accepts and returns
objects.

See [JSON](../docs/json.md) in the documentation.
