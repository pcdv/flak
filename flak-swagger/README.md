# flak-swagger

Generates an OpenAPI specification from the route handlers of a Flak
application. It fills in routes, HTTP methods, parameters and JSON schemas
automatically, and reads Swagger annotations for the rest.

See [OpenAPI](../docs/openapi.md) in the documentation.
